# تعليمات إعداد تكامل Jules API مع GitHub Actions

لتفعيل الاتصال بين مسار تشغيل GitHub Actions وواجهة برمجة تطبيقات Jules بنجاح، يجب إضافة مفتاح الـ API كـ Secret في مستودع GitHub الخاص بك.

**الرجاء اتباع الخطوات التالية بدقة:**

1.  اذهب إلى صفحة المستودع الخاص بك على **GitHub**.
2.  انقر على علامة التبويب **Settings** (الإعدادات) في القائمة العلوية.
3.  في القائمة الجانبية اليسرى، قم بالتمرير لأسفل واضغط على **Secrets and variables** ثم اختر **Actions**.
4.  في صفحة "Actions secrets and variables"، انقر على الزر الأخضر **New repository secret** (سر مستودع جديد).
5.  في حقل **Name** (الاسم)، أدخل الاسم التالي تماماً كما هو مكتوب (بدون مسافات إضافية):
    `JULES_API_KEY`
6.  في حقل **Secret** (السر)، قم بلصق مفتاح واجهة برمجة التطبيقات الخاص بك.
    *(ملاحظة: تأكد من عدم وجود مسافات فارغة قبل أو بعد المفتاح)*
7.  انقر على الزر الأخضر **Add secret** (إضافة السر) لحفظ الإعدادات.

**ملاحظات أمنية هامة:**
*   بمجرد حفظ الـ Secret، لن تتمكن أنت أو أي شخص آخر من رؤية محتواه مرة أخرى لأسباب أمنية.
*   تم إعداد ملفات الأتمتة (`.github/workflows/jules-integration.yml` والنص البرمجي `jules_integration.py`) لقراءة هذا المفتاح المشفر حصرياً، مما يمنع تسريبه في سجلات البناء (Logs).
*   لا تقم أبداً بكتابة هذا المفتاح مباشرة داخل أي ملف كود في المستودع.
